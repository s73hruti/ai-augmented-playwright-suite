// Temporary placeholder so `tsc` and `eslint` have at least one file to
// check while Phase 0 (tooling) is set up but before real source code
// exists (starting around Day 12). Safe to delete once that happens —
// tracked as a TODO so we don't forget.
export {};